# Copy to .env and adjust.
# Leave MONGO_URI empty to auto-start an in-memory MongoDB (zero setup, great for demos/dev).
# For persistence use a local mongod or MongoDB Atlas connection string.
MONGO_URI=
PORT=5000
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d
CLIENT_ORIGIN=http://localhost:5173
